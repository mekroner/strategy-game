[package]
name = "strategy-game"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "terrain_gen"

[dependencies]
bevy = "0.14.2"
bevy_egui = "0.29.0"

# Enable max optimizations for dependencies, but not for our code:
[profile.dev.package."*"]
opt-level = 3
